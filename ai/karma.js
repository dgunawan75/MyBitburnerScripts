/** @param {NS} ns */
export async function main(ns) {
    let karma = ns.heart.break();
    ns.tprint(`=========================================`);
    ns.tprint(` 😈 KARMA ANDA SAAT INI: ${ns.formatNumber(karma)}`);
    ns.tprint(`=========================================`);
    ns.tprint(`* Semakin minus, semakin jahat.`);
    ns.tprint(`* Anda butuh sekitar -54k (atau -90k tergantung update) untuk diundang ke Slum Snakes/The Syndicate.`);
    ns.tprint(`* Gunakan menu 'Crime' -> 'Homicide' untuk menurunkan Karma paling cepat.`);
}
